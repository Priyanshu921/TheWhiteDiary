{
  "name": "backend",
  "version": "1.0.0",
  "description": "the white diary backend",
  "main": "index.js",
  "type": "module",
  "scripts": {
     "start": "node --experimental-specifier-resolution=node index.js",
    "heroku-postbuild": "cd frontend && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yashi jain and priyanshu patidar",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongodb": "^4.12.1",
    "mongoose": "^6.7.5",
    "nodemon": "^2.0.20"
  }
}
